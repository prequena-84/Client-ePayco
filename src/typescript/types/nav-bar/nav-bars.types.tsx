type TLink = string;
type TLabel = string;
type TMenu = string;
type TTitleBrand = string;
type TTitleNavbar = string;

export type {
    TLink,
    TLabel,
    TMenu,
    TTitleBrand,
    TTitleNavbar,
}